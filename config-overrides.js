// @flow

module.exports = function override(config: Object) {
  //do stuff with the webpack config...
  // if(config && config.resolve && config.resolve.alias) {
  //   config.resolve.alias['joi'] = 'joi-browser'
  // }
  return config;
}
