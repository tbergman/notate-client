// @flow

// import { createSelector } from 'reselect'
// import type { AppState } from 'modules/reducers'
//
// type TasksSelector = (state: AppState) => TasksState
// export const tasksSelector:TasksSelector = (state) => state.tasks || []
